# STORY-005.001: Criar Tabelas de Analytics (daily_analytics + activity_log)

**Status:** Ready for Review
**Complexity:** 2 pontos
**Created:** 2026-03-05
**Epic:** EPIC-005: Supabase Full Integration
**Depends On:** Nenhuma (tabelas novas, não interferem com código existente)

---

## Problem Statement

O CEO Dashboard exibe dados falsos hardcoded:
- ❌ Métricas: `1,247` usuárias, `892` ativas, `156` desafios hoje, `2,834` medalhas (linhas 115, 128, 140, 152 em `CEODashboard.tsx`)
- ❌ Activity feed: "Maria Silva - há 5 minutos", "Ana Costa - há 12 minutos" (linhas 167-188)
- ❌ Gráficos com `Math.random()` em `Dashboard2Section.tsx` (linhas 116-137)

Antes de substituir esses dados, precisamos das tabelas no banco:
1. `activity_log` — registra eventos reais (challenges, medalhas, logins)
2. `daily_analytics` — agregado diário para gráficos do CEO

---

## Acceptance Criteria

### AC1: Criar tabela `activity_log`
- [ ] Migration criada: `supabase/migrations/20260305000400_create_activity_log.sql`
- [ ] SQL exato:
```sql
CREATE TABLE IF NOT EXISTS public.activity_log (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES public.profiles(id) ON DELETE CASCADE NOT NULL,
  action_type TEXT NOT NULL CHECK (action_type IN (
    'challenge_completed',
    'badge_earned',
    'level_up',
    'community_post',
    'subscription_changed',
    'user_registered',
    'streak_milestone'
  )),
  action_data JSONB DEFAULT '{}' NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);

-- Índices para queries frequentes do CEO Dashboard
CREATE INDEX IF NOT EXISTS idx_activity_log_user_id ON public.activity_log(user_id);
CREATE INDEX IF NOT EXISTS idx_activity_log_created_at ON public.activity_log(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_activity_log_action_type ON public.activity_log(action_type);

-- RLS
ALTER TABLE public.activity_log ENABLE ROW LEVEL SECURITY;

-- CEO vê tudo
CREATE POLICY "CEO can read all activity" ON public.activity_log
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'ceo')
  );

-- Usuário vê próprio histórico
CREATE POLICY "Users can read own activity" ON public.activity_log
  FOR SELECT USING (auth.uid() = user_id);

-- Apenas o próprio usuário pode inserir (sistema também via service role)
CREATE POLICY "Users can insert own activity" ON public.activity_log
  FOR INSERT WITH CHECK (auth.uid() = user_id);

-- Ninguém deleta (audit trail)
-- (sem DELETE policy = nenhum usuário pode deletar)

COMMENT ON TABLE public.activity_log IS 'Audit trail de todas as ações dos usuários. Usado pelo CEO Dashboard para feed de atividade recente.';
COMMENT ON COLUMN public.activity_log.action_data IS 'JSON com dados específicos da ação. Ex: {challenge_id, points_earned, badge_name}';
```

### AC2: Criar tabela `daily_analytics`
- [ ] Migration criada no mesmo arquivo `20260305000400_create_activity_log.sql` (apender ao final)
- [ ] SQL exato:
```sql
CREATE TABLE IF NOT EXISTS public.daily_analytics (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  date DATE NOT NULL UNIQUE,
  active_users INT DEFAULT 0 NOT NULL,
  new_users INT DEFAULT 0 NOT NULL,
  challenges_completed INT DEFAULT 0 NOT NULL,
  community_posts INT DEFAULT 0 NOT NULL,
  badges_earned INT DEFAULT 0 NOT NULL,
  revenue_brl DECIMAL(10,2) DEFAULT 0 NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL,
  updated_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);

-- Índice para range queries por data
CREATE INDEX IF NOT EXISTS idx_daily_analytics_date ON public.daily_analytics(date DESC);

-- RLS
ALTER TABLE public.daily_analytics ENABLE ROW LEVEL SECURITY;

-- Apenas CEO lê analytics
CREATE POLICY "CEO can read analytics" ON public.daily_analytics
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'ceo')
  );

-- Sistema insere/atualiza via service role (não precisa de policy para INSERT)

COMMENT ON TABLE public.daily_analytics IS 'Agregado diário de métricas. Populado por job ou trigger. Usado nos gráficos do CEO Dashboard.';
```

### AC3: Seed histórico dos últimos 30 dias
- [ ] Migration separada: `supabase/migrations/20260305000401_seed_daily_analytics.sql`
- [ ] SQL que calcula dados reais dos últimos 30 dias a partir das tabelas existentes:
```sql
-- Insere analytics dos últimos 30 dias calculando a partir de dados reais
INSERT INTO public.daily_analytics (date, active_users, new_users, challenges_completed, badges_earned)
SELECT
  d.date,
  -- Usuários que completaram ao menos 1 desafio nesse dia
  COALESCE(cc.active_users, 0) AS active_users,
  -- Usuários que criaram perfil nesse dia
  COALESCE(p.new_users, 0) AS new_users,
  -- Desafios completados nesse dia
  COALESCE(cc.challenges_count, 0) AS challenges_completed,
  -- Badges ganhos nesse dia
  COALESCE(b.badges_count, 0) AS badges_earned
FROM
  generate_series(
    CURRENT_DATE - INTERVAL '30 days',
    CURRENT_DATE,
    '1 day'
  ) AS d(date)
LEFT JOIN (
  SELECT
    DATE(completed_at) AS day,
    COUNT(DISTINCT user_id) AS active_users,
    COUNT(*) AS challenges_count
  FROM public.challenge_completions
  WHERE completed_at >= CURRENT_DATE - INTERVAL '30 days'
  GROUP BY DATE(completed_at)
) cc ON cc.day = d.date
LEFT JOIN (
  SELECT
    DATE(created_at) AS day,
    COUNT(*) AS new_users
  FROM public.profiles
  WHERE role = 'user' AND created_at >= CURRENT_DATE - INTERVAL '30 days'
  GROUP BY DATE(created_at)
) p ON p.day = d.date
LEFT JOIN (
  SELECT
    DATE(earned_at) AS day,
    COUNT(*) AS badges_count
  FROM public.user_earned_badges
  WHERE earned_at >= CURRENT_DATE - INTERVAL '30 days'
  GROUP BY DATE(earned_at)
) b ON b.day = d.date
ON CONFLICT (date) DO UPDATE SET
  active_users = EXCLUDED.active_users,
  new_users = EXCLUDED.new_users,
  challenges_completed = EXCLUDED.challenges_completed,
  badges_earned = EXCLUDED.badges_earned,
  updated_at = NOW();
```

### AC4: Adicionar métodos ao SupabaseClient
- [ ] `getActivityLog(limit = 10)` adicionado em `src/lib/supabase-client.ts`:
```typescript
async getActivityLog(limit = 10): Promise<ActivityLogEntry[]> {
  try {
    const { data, error } = await supabase
      .from('activity_log')
      .select(`
        *,
        user_profile:profiles(id, full_name, avatar_url)
      `)
      .order('created_at', { ascending: false })
      .limit(limit)
    if (error) throw error
    return data || []
  } catch (err) {
    console.error('❌ getActivityLog error:', err)
    return []
  }
}
```
- [ ] `getDailyAnalytics(days = 30)` adicionado em `src/lib/supabase-client.ts`:
```typescript
async getDailyAnalytics(days = 30): Promise<DailyAnalytics[]> {
  try {
    const { data, error } = await supabase
      .from('daily_analytics')
      .select('*')
      .order('date', { ascending: true })
      .limit(days)
    if (error) throw error
    return data || []
  } catch (err) {
    console.error('❌ getDailyAnalytics error:', err)
    return []
  }
}
```
- [ ] Interfaces TypeScript adicionadas no topo de `supabase-client.ts`:
```typescript
export interface ActivityLogEntry {
  id: string
  user_id: string
  action_type: 'challenge_completed' | 'badge_earned' | 'level_up' | 'community_post' | 'subscription_changed' | 'user_registered' | 'streak_milestone'
  action_data: Record<string, any>
  created_at: string
  user_profile?: { id: string; full_name: string; avatar_url?: string }
}

export interface DailyAnalytics {
  id: string
  date: string
  active_users: number
  new_users: number
  challenges_completed: number
  community_posts: number
  badges_earned: number
  revenue_brl: number
  created_at: string
  updated_at: string
}
```

### AC5: Registrar eventos automaticamente
- [ ] Em `ChallengesSection.tsx` (dentro de `savePointsAndCheck()`), após `recordChallengeCompletion()`, adicionar:
```typescript
// Registra no activity_log
await supabaseClient.logActivity(user.id, 'challenge_completed', {
  challenge_id: challengeId,
  points_earned: pointsFromChallenge,
  category_id: activeCategory.id,
  difficulty
})
```
- [ ] Adicionar `logActivity()` ao SupabaseClient:
```typescript
async logActivity(userId: string, actionType: ActivityLogEntry['action_type'], data: Record<string, any> = {}): Promise<void> {
  try {
    await supabase.from('activity_log').insert({
      user_id: userId,
      action_type: actionType,
      action_data: data,
    })
  } catch (err) {
    console.error('❌ logActivity error:', err) // Não lança — log é não-crítico
  }
}
```

### AC6: Quality Gates
- [ ] `npm run build` — 0 TypeScript errors
- [ ] `npm run lint` — 0 warnings
- [ ] Migration é idempotente (IF NOT EXISTS em todas as DDLs)
- [ ] Verificação manual: tabela `activity_log` existe no Supabase dashboard
- [ ] Verificação manual: tabela `daily_analytics` existe com linhas dos últimos 30 dias
- [ ] Completar um desafio → nova row em `activity_log`

---

## Scope

### IN Scope ✅
- Criar as 2 novas tabelas
- RLS policies nas 2 tabelas
- Seed analytics dos últimos 30 dias
- Interfaces TypeScript para as novas tabelas
- Métodos `getActivityLog()`, `getDailyAnalytics()`, `logActivity()`
- Registrar eventos de challenge_completed automaticamente

### OUT of Scope ❌
- Exibir os dados no CEO Dashboard (isso é STORY-005.010)
- Registrar outros tipos de eventos além de `challenge_completed` (pode vir depois)
- Trigger automático do PostgreSQL para atualizar daily_analytics (complexidade alta, job manual suficiente por ora)
- Realtime publication para estas tabelas (CEO não precisa de realtime nestas)

---

## Risks & Blockers

### Risk 1: Migration em banco de produção (Alto)
- **Problema:** Supabase direto da WSL2 não é acessível via psql/pooler (apenas via IPv6).
- **Mitigação:** Usar Management API com PAT para executar o SQL:
```bash
curl -X POST "https://api.supabase.com/v1/projects/wrcprmujlkwvusekfogf/database/query" \
  -H "Authorization: Bearer $(grep SUPABASE_ACCESS_TOKEN .env | cut -d= -f2)" \
  -H "Content-Type: application/json" \
  -d '{"query": "... SQL aqui ..."}'
```
- **Alternativa:** Rodar via Supabase Dashboard → SQL Editor
- **Impacto:** Sem isso, tabelas não existem e STORY-005.010 bloqueia.

### Risk 2: Seed analytics pode dar 0 para todos os dias (Baixo)
- **Problema:** Se não há `challenge_completions` nem `user_earned_badges` no banco, todos os dias vão ter 0.
- **Mitigação:** Isso é esperado para ambiente vazio — as queries retornam 0 por COALESCE, não falham.
- **Impacto:** Gráfico do CEO vai mostrar linha zerada, mas é correto. Não é bug.

### Risk 3: `action_type` CHECK constraint pode bloquear novos tipos (Baixo)
- **Problema:** Se no futuro precisar de novo tipo de evento, a constraint impede.
- **Mitigação:** O dev pode adicionar migration para expandir o CHECK. Documentar isso.
- **Impacto:** Baixo — é planejado, não é surpresa.

---

## Task Breakdown

### Task 5.1.1: Criar migrations
**Owner:** @data-engineer
**Effort:** 0.75 pontos

- [ ] Escrever `20260305000400_create_activity_log.sql` com as duas DDLs
- [ ] Escrever `20260305000401_seed_daily_analytics.sql`
- [ ] Executar via Management API (não via psql — WSL2 não conecta)
- [ ] Verificar no Supabase Dashboard que tabelas criadas com RLS habilitado

### Task 5.1.2: Adicionar interfaces e métodos ao SupabaseClient
**Owner:** @dev
**Effort:** 0.75 pontos

- [ ] Adicionar interfaces `ActivityLogEntry` e `DailyAnalytics` em `supabase-client.ts`
- [ ] Implementar `getActivityLog()`, `getDailyAnalytics()`, `logActivity()`
- [ ] Integrar `logActivity()` em `ChallengesSection.tsx`
- [ ] Build e lint passam

---

## File List

**Criados:**
- `supabase/migrations/20260305000400_create_activity_log.sql`
- `supabase/migrations/20260305000401_seed_daily_analytics.sql`

**Modificados:**
- `src/lib/supabase-client.ts` (+3 métodos, +2 interfaces, ~60 linhas)
- `src/components/user/sections/ChallengesSection.tsx` (+logActivity call, ~4 linhas)

---

## Testing

- [ ] Tabela `activity_log` aparece no Supabase Dashboard com RLS ativado
- [ ] Tabela `daily_analytics` aparece com linhas (mesmo que sejam zeros)
- [ ] Completar um desafio → SELECT * FROM activity_log → nova row aparece
- [ ] CEO logado pode fazer SELECT da activity_log (RLS permite)
- [ ] User logado NÃO consegue fazer SELECT de activity de outro usuário (RLS bloqueia)
- [ ] `npm run build` → 0 errors
- [ ] `npm run lint` → 0 warnings

---

## Quality Gates

- [ ] `npm run build` - 0 TypeScript errors
- [ ] `npm run lint` - 0 warnings
- [ ] Migrations são idempotentes (rodar 2x não quebra)
- [ ] RLS testada com dois usuários diferentes
- [ ] Activity log registra eventos após challenge completion

---

## Definition of Done

**COMPLETO quando:**
- [ ] Task 5.1.1 feita: 2 migrations executadas no Supabase
- [ ] Task 5.1.2 feita: 3 métodos no SupabaseClient, logActivity integrado
- [ ] Build 0 errors, lint 0 warnings
- [ ] AC1-AC6 verificados manualmente

---

## Dev Agent Record

### Progress Checkboxes
- [x] Task 5.1.1: Criar migrations
- [x] Task 5.1.2: Adicionar ao SupabaseClient
- [x] Quality gates passing

### Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-03-05 | @sm | Story criada — EPIC-005 DB Tables |
| 2026-03-05 | @data-engineer | Task 5.1.1 concluída: migrations criadas e executadas no Supabase. 5/5 RLS tests ✅ |
| 2026-03-05 | @dev | Task 5.1.2 concluída: interfaces + 3 métodos + logActivity integrado. Build ✅ Lint ✅ |
